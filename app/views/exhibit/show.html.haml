- @page_title = '出品商品画面'
- breadcrumb :mypage

= content_for :header do
  = render 'shared/header'
= content_for :app_banner do
  = render 'shared/app-banner'
= content_for :footer do
  = render 'shared/footer'

.item-box__container
  %main.main
    .main__container__edit-item-box
      %section.main__container__sell-item-box
        %h1.sell-item__name
          = @item.name
        .sell-item__main-content
          .sell-item__photo
            .sell-item__photo__all
              .sell-item__photo__main
                -# - top_image =@item.images.first
                -# = image_tag "#{top_image.image}", alt: "", class: ""
              .sell-item__photo__dots
                -# .sell-item__photo__dot.active
                  -# .sell-item__photo__dot__inner
                    -# = image_tag "#{@top_image.image}", alt: "", class: ""
                    -# = image_tag "ruffy.jpeg", alt: "", class: ""
                - @item.images.each_with_index do |image, i|
                  = render partial:'items/item-photo', locals: {image: image}
                  -# - for i in 1..4
                    -# = render partial:'items/item-photo', locals: {image: image}
                  -# - for i in 1..2
                  -#   = render partial:'items/item-photo', locals: {image: image}
          -# .sell-item__price-box.display-none
          -#   %span.sell-item__price¥499
          -#   %span.sell-item__tax（税込）
          -#   %span.sell-item__shipping-fee 送料込み
          %table.sell-item__detail-table
            %tbody
              %tr
                %th 出品者
                %td
                  = link_to "ほげ太", "#"
                  %div
                    .sell-item__user-ratings
                      .icon-good
                        %i.fa.fa-laugh
                        %span 258
                    .sell-item__user-ratings
                      .icon-normal
                        %i.fa.fa-meh
                        %span 2
                    .sell-item__user-ratings
                      .icon-bad
                        %i.fa.fa-frown
                        %span 0
              %tr
                %th カテゴリー
                %td
                  = link_to @item.category.name
                  -# = link_to Categoryt.all[@item.category_id].name
                  .sub-category
                    = link_to "#" do
                      %i.fa.fa-angle-right
                      %span サブカテ1
                  .sub-category
                    = link_to "#" do
                      %i.fa.fa-angle-right
                      %span サブカテ2
              %tr
                %th ブランド
                %td
              %tr
                %th 商品の状態
                %td 
                  =@item.state.name
                  -# =Statet.all[@item.postage_burden].name
              %tr
                %th 配送料の負担
                %td 
                  = @item.postage_burden.name
                  -# =Burdent.all[@item.postage_burden].name
              %tr
                %th 配送の方法
                %td 
                  =@item.shipping_way.name
              %tr
                %th 配送元地域
                %td
                  = @item.prefecture.name
                  -# = link_to Prefecture.all[@item.postage_burden].name
              %tr
                %th 発送日の目安
                %td 
                  = @item.shipping_date.name
                  -# =Dateh.all[@item.postage_burden].name
          .sell-item__price-box
            %span.sell-item__price 
              ¥#{@item.price}
            %span.sell-item__tax  (税込)
            %span.sell-item__shipping-fee 送料込み
        .item-description
          %p.item-description__inner{style: "font-size: 18px"}
            =@item.comment
      .item-change__box
        %button.btn-default.btn-red{type: "submit"} 
          = link_to "商品の編集","/items/#{@item.id}/edit"
        %p 
          or
        %button.btn-default.btn-gray{type: "submit"} 
          = link_to "出品を一旦停止する","#"
        %button.btn-default.btn-gray{type: "submit"} 
          = link_to "この商品を削除する","#"
      .item-detail__message
        .message__container
          = render 'items/message'
          .message__space
          .message__content
            %form{action: '', method: 'POST', class:'.message__form'}
              %p
                相手のことを考え丁寧なコメントを心がけましょう。不快な言葉遣いなどは利用制限や退会処分となることがあります。
              %textarea{name: 'body', class: '.textarea-default'}
              %button{type: 'submit', class: 'btn-bray'}
                %span コメントする
          %aside.model.animate
            .model-inner
              .model-body
                .model-head.bold
                  確認
                  %p
                    削除すると２度と復活できません。
                    %br
                    削除する代わりに停止することもできます
                    %br
                    %br
                    本当に削除しますか?
                .clearfix
                  .model-btn.model-btn-cancel キャンセル
                  %button{type: 'submit', class: 'modal-btn modal-btn-submit'}
                
