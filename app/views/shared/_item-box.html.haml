.itembox
  -# todo: 仮置き サーバーサイド実装後に正式なパスを設定
  = link_to item_path(1) do
    %figure.itembox__photo
      = image_tag '', alt: '#'
    .itembox__body
      %h3.itembox__name @item.name
      .itembox__num
        %p.itembox__price @item.price
        %p.itembox__like
          %i.far.fa-heart
          3